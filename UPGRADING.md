# Upgrading

## From v1 to v2

- Install `wotz/filament-redirects` instead of `codedor/filament-redirects`
- Replace all occurrences of `Codedor\FilamentRedirects` namespace with new `Wotz\FilamentRedirects` namespace
